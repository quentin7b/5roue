{
  "name": "api",
  "version": "1.0.50",
  "scripts": {
    "migrate": "bunx prisma format && bunx prisma migrate dev --name init && bunx prisma generate",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts"
  },
  "dependencies": {
    "@elysiajs/bearer": "^0.8.0",
    "@elysiajs/jwt": "^0.8.0",
    "@elysiajs/swagger": "^0.8.3",
    "@prisma/client": "5.8.0",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prisma": "^5.8.0"
  },
  "module": "src/index.js"
}